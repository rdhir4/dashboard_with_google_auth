import styled from "styled-components";

const StyledloginFormHeader = styled.div`
    margin-bottom: 25px;
    .da-header-title-cn {
        margin-bottom: 26px;
        .da-header {
            font: 36px Montserrat;
            font-weight: 700;
            margin-bottom: 5px;
        }
        .da-desc {
            font: 16px Lato;
        }
    }
`;

export default StyledloginFormHeader