import styled from "styled-components";

const StyledLoginSidebar = styled.div`
    .da-sidebar-title{
        font: 72px Montserrat;
        font-weight: 700;
        color: var(--whiteColor);
        background-color: var(--blackColor);
        height: 100vh;
        width: 588px;
        display: flex;
        justify-content: center;
        align-items: center;
    }
`

export default StyledLoginSidebar